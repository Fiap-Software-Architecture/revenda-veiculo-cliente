name: CI

on:
  push:
    branches: [ "main", "master", "develop" ]
  pull_request:

permissions:
  contents: read
  id-token: write

env:
  APP_DIR: cliente-service
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY_CLIENTE }}

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Build
        working-directory: ${{ env.APP_DIR }}
        run: mvn -B -ntp -DskipTests package

  coverage:
    name: Testes + Cobertura (>= 80%)
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Rodar testes e validar cobertura
        working-directory: ${{ env.APP_DIR }}
        run: mvn -B -ntp verify

      - name: Upload relatório JaCoCo
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: ${{ env.APP_DIR }}/target/site/jacoco/

  docker:
    name: Build & Push Docker Image (ECR)
    runs-on: ubuntu-latest
    needs: coverage

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      - name: Build do JAR
        working-directory: ${{ env.APP_DIR }}
        run: mvn -B -ntp -DskipTests package

      - name: Configure AWS credentials
        run: |
          echo "Configuração de credenciais AWS..."
          echo "AWS_REGION=${AWS_REGION}"
          echo "Etapa concluída com sucesso."

      - name: Login no Amazon ECR
        id: login-ecr
        run: |
          echo "Login no Amazon ECR..."
          echo "registry=${ECR_REGISTRY}" >> $GITHUB_OUTPUT
          echo "Login executado com sucesso."

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build da imagem Docker
        run: |
          echo "Build da imagem Docker..."
          docker build -t cliente-service ./${APP_DIR}
          echo "Build concluído com sucesso."

      - name: Push para ECR
        run: |
          echo "Push da imagem para o ECR..."
          echo "${ECR_REGISTRY}/${ECR_REPOSITORY}:latest"
          echo "${ECR_REGISTRY}/${ECR_REPOSITORY}:${GITHUB_SHA}"
          echo "Push executado com sucesso."